# LukeMorton.co.uk

Adding ssh key:

``` sh
ssh-add private/keys/lukemorton
```

Running dev server:

``` sh
make dev
```

Provisioning (includes deploy):

``` sh
make
```

Deploying:

``` sh
make deploy
```
